class Config:
    YM_PER_PIX = 30 / 720   # meters per pixel in vertical (Y) direction
    XM_PER_PIX = 3.7 / 700  # meters per pixel in horizontal (X) direction

    # Number of frames for temporal averaging
    TIME_WINDOW = 10
